# 代码清理记录（2026-09-18）

本次只清理当前工程确认未使用的代码，未修改模型设置、Neo4j 数据、课程源文件或聊天内容。

## 已清理

- `unified_graph.py`：删除无调用的 `search_knowledge()` 和 `scoped_course_context()`；同步移除 `neo4j_client.py` 的相关导出及未使用的 `database` 导出。
- `graph_routes.py`：删除当前前端已无调用的 `/api/graph/courses/{course_id}/supplements` 旧列表接口及仅供它使用的导入。旧版知识本身仍可参与正常问答及关联查询。
- `static/app.js`：删除始终未传入数据的模型名、Token 数展示分支，简化消息函数参数并同步所有调用；删除始终隐藏的头像与发送者标签，使用消息的无障碍标签保留角色信息。
- `static/style.css`：删除上述废弃元素的专属样式；更新页面资源版本以加载清理后的代码。
- 旧 `neo4j_legacy_client.py` 已移至 `archive/legacy/`，并核验归档前后内容一致；使用说明已更新路径。

## 继续保留

- 首次清理时保留了 Streamlit 备用网页；用户随后确认不再使用，已按下方记录停用。命令行入口、人工调试及验收工具继续保留。
- 旧的 `draft` 链接处理：仍可用于打开以前保存的提问草稿。
- 测试、导图转换和数据导入工具、第三方渲染依赖、原始及生成的课程数据。

修改前的七个文件已备份于 `archive/cleanup-before-20260918-091955.zip`。

## 验证结果

- 105 项自动测试全部通过；Python 文件语法检查通过。
- 浏览器检查通过：历史聊天正常载入，回答的知识图谱依据可展开；高等数学原图、599 个可点击知识点、详情及提问链接正常显示。
- 检查过程中未发现页面脚本错误；本次未重新调用大模型生成回答。

## 后续：停用旧版 Streamlit 网页

用户确认不再使用旧版网页后，将 `web_app.py`、`start_streamlit.bat`、`.streamlit/config.toml` 和 `tests/test_web_app.py` 按原相对路径移至 `archive/streamlit-retired/`。同步删除依赖清单中的 Streamlit，并从项目虚拟环境卸载该包；更新当前启动说明。归档文件仅供历史对照，不属于当前运行入口。

移除旧网页的 5 项专用测试后，其余 100 项测试全部通过；当前服务健康检查正常，Neo4j 连接正常。模型配置、聊天记录及图谱数据未修改。
