# 26门课程知识图谱问答与导图浏览

## 启动

1. 在 Neo4j Desktop 中启动已导入统一图谱的数据库。
2. 双击 `kg_qa_system/start_web.bat`。
3. 打开 http://127.0.0.1:8000 进入问答；打开 http://127.0.0.1:8000/graph 浏览导图。

本地服务仅监听 127.0.0.1，不对局域网或公网开放。旧版 Streamlit 网页已停用并归档，统一使用 `start_web.bat` 启动。

## 问答

- 默认在26门课程中检索，也可以选择单门课程。
- 从导图中的“围绕这个知识点提问”进入时，页面显示“当前导图”，优先读取当前导图的文字、备注和相关分支，Neo4j 中核对后的对应资料作为补充。
- 点击“取消知识点限定”可恢复课程范围内的检索；切换课程也会取消知识点限定。
- 回答下方可展开“知识图谱依据”，查看来源、关系和方向，并跳转到对应知识点。
- 使用 `.env` 中原先配置的 `FAST_MODEL`、`STRONG_MODEL` 及千问接口；未更换模型。
- 聊天记录会保存，最近的对话参与理解追问；重新打开导图聊天可恢复对应导图来源。点击“取消限定”或新建聊天可以恢复普通问答。

## 知识导图

网页只显示原图交互，已接入 26 门课程及“26门课程总览”的 SVG，详见 [知识导图接入说明](知识导图接入说明.md)。

- 左侧选择课程；支持课程名称筛选。
- 拖动浏览原图；使用“＋”“−”或 Ctrl + 滚轮缩放，“全图”查看整体，“100%”恢复原尺寸。
- 点击节点查看详情，包括文字、知识路径、来源、关联和图片；可继续围绕该知识点提问。较窄窗口中详情显示在导图下方。
- 搜索原图中的知识点，点击结果返回原图并定位；清空搜索可回到原图。结果最多展示 200 项，可缩小搜索范围。
- 数据库显示上传的精简原图。若问答或关联链接指向原图未包含的知识点，资料仍显示在详情区。
- “回到课程起点”定位课程根节点。原图不支持分支折叠或编辑。

## 当前数据与检索边界

统一图谱共有26门课程、22,891个实体、32,606条关系，其中新版主知识节点18,391个，图片1,039张。实体总数还包含注释、辅助结构及旧版补充资料。

运行代码从本地数据包计算版本编号，只检索 Neo4j 中状态为 `ready` 的对应版本，不混入其他版本及原 `KGNode` 数据。旧版CSV内容已作为 `LEGACY:` 来源纳入新版图谱。网页导图优先读取 `web_mindmaps` 中由新 EMMX 转换的课程文件，保留与数据库一致的课程知识点编号。本地数据包保留 1,956 条图片记录和 5 份总览／联系图，网页入口仅展示“26门课程总览”；新增内容尚未导入问答数据库。

检索使用实体名称、别名和包含匹配，结合指定关系及邻近节点、两到三级下级内容取证，尚未加入向量检索。跨课程总览关系经课程入口映射参与上下文。语义关系保留原文字；无方向、双向和反向连线分别处理，不推断为先修关系。图片内容尚未进行OCR或视觉理解。

## 常用检查

```powershell
cd kg_qa_system
.\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v
```

这是离线测试，不调用模型，不产生模型费用。健康接口为 `/api/health`，课程接口为 `/api/graph/courses`，接口文档为 `/docs`。

如果图谱健康但问答失败，请检查千问接口可达性、API密钥、额度和模型权限。启动日志与配置仅在本机查看，不将真实密钥粘贴进网页或提交版本库。

新版本导入见 `unified_kg/Neo4j接入说明.md`。旧版数据库访问代码保存于 `archive/legacy/neo4j_legacy_client.py` 供对照，当前应用入口使用 `unified_graph.py`。
