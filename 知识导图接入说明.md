# 新上传 EMMX 导图的网页接入

## 全部 SVG 原图交互

网页已接入 `知识导图svg` 中的 27 份 SVG：26 门课程和“26门课程总览”。支持拖动、缩放、全图、搜索定位和点击详情／提问，共 18,095 个可点击区域。除数据库精简版外，25 门课程和总览的主知识点均已按源编号及文字或图片内容核对。页面只保留原图交互，已移除内容类型切换菜单及其他展示模式。当前不支持原图分支折叠或编辑。

数据库 SVG 是精简版，与完整课程的源编号有冲突。网页默认展示精简原图，除课程根节点外使用独立的 `SVG:C12:` 编号；点击“围绕这个知识点提问”直接以当前导图为上下文，使用独立的导图来源标记，不把原图编号当成 Neo4j 编号。原来的完整课程资料仍保留；从问答或关联链接进入原图未包含的知识点时，在详情区显示资料，不切换为其他导图模式。

操作系统、计算机网络、编译原理、人工智能四份 SVG 有元素落在原画布外，显示副本仅补足根画布边界，图形元素、文字、位置、颜色和连线不变。上传原文件和原始副本保持不变。大图使用预压缩传输，解压后的显示内容保持一致。

另外四份补充联系图（唐晓松课程总览、导图、汇总、计网-编译-物联网-人工智能联系图）已按用户要求从网页入口移除，原始文件保留。菜单只显示 26 门课程及“26门课程总览”。模型、Neo4j 和原课程数据均未改变，图片没有自动进入模型检索。

在项目根目录运行 `kg_qa_system/.venv/Scripts/python.exe tools/build_original_maps.py`，可重新生成全部原图显示版本和点击区域。`tools/build_high_math_svg.py` 仍可仅重建高数。转换器先验证整批文件再发布，记录见 `web_mindmaps/original/validation.json`。完成后重启网页服务并刷新。此功能使用 SVG 和现有课程数据，不代表新版 EMMX 的二进制解析已经完成。

## EMMX 转换数据

网页导图现在优先读取 `web_mindmaps`，该目录由 `知识导图` 文件夹转换生成。
EMMX 是编辑器文件；网页仍通过 JSON 读取文字、层级、关系和图片，不是把扩展名改成 JSON。

- 25 门课程的正文、备注及图片来自新 EMMX；使用原有节点编号及经核对的层级，保留问答跳转。
- 高等数学的 EMMX 使用新版二进制格式，未完成直接解析；当前已使用提供的 SVG 进行原图交互，详情保持原兼容数据。
- 包含 5 份独立总览／联系图，按原位置显示节点，保留连接方向；它们未自动合并进 Neo4j。
- 配图记录从 1,039 条增加为 1,956 条；新增 917 次图片引用，对应 911 张不重复的图片内容。备注中的图文按段落顺序显示，图片可查看原图。
- `unified_kg`、原始资料、Neo4j 关系和模型配置未更改。普通聊天继续检索原数据库；导图提问优先读取当前导图资料，新增图片尚未经过文字识别。

## 更新转换结果

在项目根目录执行：

```powershell
& .\kg_qa_system\.venv\Scripts\python.exe -X utf8 tools/build_mindmaps.py
```

转换器核对原节点和压缩后的父子关系，提取新正文及备注，校验原配图内容，遇到不匹配时停止并报错。
完成后重启网页服务，再刷新页面。不要手动替换 `unified_kg/manifest.json`；它参与数据库版本计算。

导图媒体保存在 `web_mindmaps/media`，部署时应与课程数据一起带走。

## 导图提问的数据来源

点击知识点的提问按钮后，页面显示“当前导图”，发送 node_id 和 context_source=mindmap。后端从白名单中的原图及已转换课程资料读取名称、课程、知识路径、正文、备注、相关分支和关联说明。高等数学直接读取 SVG 保存的父子结构与备注，不使用兼容 EMMX 副本推测当前原图层级。数据库精简原图与完整课程资料始终分开，共享根节点也不混入完整版本分支。

Neo4j 仅提供补充：先核对课程、名称和完整知识路径，再查询对应节点及问题所需关系；编号相同不能替代核对。无法确认或数据库不可用时，仍以本地导图资料交给原配置模型回答。相关节点名称和路径经简单规范化后精确匹配，暂不自动认定模糊名称或不同层级等价。

首轮指代解析会接收用户选中的知识点位置；后续结合聊天历史。聊天库新增 context_source 字段，旧聊天保留原来源，新聊天保存导图来源；“取消限定”和新建聊天清除当前来源限定。回答的“参考资料”中保留导图来源及返回知识点的链接。

上下文按问题优先选取相关分支，最多 80 个分支节点及约 22,000 字符，超出会标记截取；模型可根据用户要求补充讲解。图片未做 OCR 或视觉识别，不能声称读取了图片里的公式或文字。SVG 外观和 Neo4j 结构未改动。
